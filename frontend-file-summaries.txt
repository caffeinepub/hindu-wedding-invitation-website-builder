{
  "frontend/src/components/effects/MouseTiltHero.tsx": "Wrapper component that applies perspective tilt transform to hero sections based on mouse position, now includes a `style` prop to pass CSS styles directly to the container div.",
  "frontend/src/components/invitation/CountdownTimer.tsx": "Live countdown timer showing days/hours/minutes/seconds with glowing flip-card style units; fixed import order so `cn` is imported before use, and fixed the interval dependency to use `weddingDate` string directly.",
  "frontend/src/components/invitation/ShareSection.tsx": "Share section displaying a QR code (via qrserver.com API — no external npm package needed) and a copy-to-clipboard URL button with visual feedback.",
  "frontend/src/components/creator/StepPhotoUpload.tsx": "Multi-photo uploader for bride, groom, and cover images; now uses `ExternalBlob.fromBytes()` from the blob-storage component instead of Base64 conversion, storing `ExternalBlob` instances with upload progress tracking and local object URL previews.",
  "frontend/src/components/creator/StepGalleryMusic.tsx": "Gallery multi-upload with drag-and-drop reordering and background music upload; uses a `useRef` to track the latest gallery items so async upload progress callbacks can call `onGalleryChange` with a plain resolved array instead of a functional updater, fixing the TypeScript type mismatch.",
  "frontend/src/pages/CreateInvitePage.tsx": "Full 6-step invite creator page managing form state and payload assembly; updated `FormState` to hold `ExternalBlob | null` for photos and `GalleryItem[]` for gallery, and `buildPayload()` now passes `ExternalBlob` instances directly to the backend without any Base64 serialization.",
  "frontend/src/pages/InvitePage.tsx": "Invitation display page at `/invite/:id` that fetches invite data by ID, lazily loads the correct template component, renders OG meta tags, and handles loading/not-found states.",
  "frontend/src/index.css": "Global styles with Google Fonts imports (Cormorant Garamond, EB Garamond, Playfair Display, Cinzel, Raleway, Lato), OKLCH color tokens for the Hindu wedding palette, utility classes for gold/ivory/crimson colors, float/mandala animations, grain texture overlay, and reduce-motion support.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Hindu wedding color palette (gold, saffron, marigold, crimson, ivory, deepMaroon, bronze, indigo), custom font families (cormorant, garamond, playfair, cinzel, raleway, lato), and animation keyframes (float, pulse-glow, mandala-spin, fade-in-up).",
  "frontend/src/lib/utils.ts": "Utility functions including `cn` for class merging, `generateId`, `formatDate`, `formatDateTime`, and the new `fileToBase64` helper that converts a File object to a persistent Base64 data URL string using FileReader.",
  "frontend/src/components/invitation/InviteMetaTags.tsx": "Sets Open Graph and Twitter meta tags for the invitation page; updated to call `invite.coverPhoto.getDirectURL()` (since `coverPhoto` is now an `ExternalBlob`) instead of comparing it as a string, with a safe try/catch fallback to the default OG image.",
  "frontend/src/components/templates/BaseTemplate.tsx": "Shared base template rendering hero, couple photos, events, gallery, RSVP, and footer sections; updated to handle both `ExternalBlob` instances (calling `getDirectURL()`) and legacy string URLs for all image fields, with helper functions `getBlobUrl` and `isValidGalleryBlob` for safe URL extraction."
}