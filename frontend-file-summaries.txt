{
  "frontend/src/components/effects/MouseTiltHero.tsx": "Wrapper component that applies perspective tilt transform to hero sections based on mouse position, now includes a `style` prop to pass CSS styles directly to the container div.",
  "frontend/src/components/invitation/CountdownTimer.tsx": "Live countdown timer showing days/hours/minutes/seconds with glowing flip-card style units; fixed import order so `cn` is imported before use, and fixed the interval dependency to use `weddingDate` string directly.",
  "frontend/src/components/invitation/ShareSection.tsx": "Share section with copy-link, WhatsApp, and QR code buttons; the \"View RSVP Responses\" button is conditionally rendered only for the authenticated creator of the invite.",
  "frontend/src/components/creator/StepPhotoUpload.tsx": "Multi-photo uploader for bride, groom, and cover images; now uses `ExternalBlob.fromBytes()` from the blob-storage component instead of Base64 conversion, storing `ExternalBlob` instances with upload progress tracking and local object URL previews.",
  "frontend/src/components/creator/StepGalleryMusic.tsx": "Gallery multi-upload with drag-and-drop reordering and background music upload; uses a `useRef` to track the latest gallery items so async upload progress callbacks can call `onGalleryChange` with a plain resolved array instead of a functional updater, fixing the TypeScript type mismatch.",
  "frontend/src/pages/CreateInvitePage.tsx": "Six-step invite creator with auth guard; reads an optional `editId` search param, fetches the existing invite via `useGetInvite`, and pre-populates all form state (template, couple details, photos, events, gallery, music) from the fetched record using the exact existing component prop interfaces (`selectedId`/`onSelect`, `data`/`onChange`, `onBrideUpload`, etc.).",
  "frontend/src/pages/InvitePage.tsx": "Invitation display page that lazily loads the correct template component via `TemplateComponents` map using `lazy()` per render, with a loading skeleton and not-found fallback.",
  "frontend/src/index.css": "Global styles with Google Fonts imports (Cormorant Garamond, EB Garamond, Playfair Display, Cinzel, Raleway, Lato), OKLCH color tokens for the Hindu wedding palette, utility classes for gold/ivory/crimson colors, float/mandala animations, grain texture overlay, and reduce-motion support.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Hindu wedding color palette (gold, saffron, marigold, crimson, ivory, deepMaroon, bronze, indigo), custom font families (cormorant, garamond, playfair, cinzel, raleway, lato), and animation keyframes (float, pulse-glow, mandala-spin, fade-in-up).",
  "frontend/src/lib/utils.ts": "Utility functions including `cn` for class merging, `generateId`, `formatDate`, `formatDateTime`, and the new `fileToBase64` helper that converts a File object to a persistent Base64 data URL string using FileReader.",
  "frontend/src/components/invitation/InviteMetaTags.tsx": "Sets Open Graph and Twitter meta tags for the invitation page; updated to call `invite.coverPhoto.getDirectURL()` (since `coverPhoto` is now an `ExternalBlob`) instead of comparing it as a string, with a safe try/catch fallback to the default OG image.",
  "frontend/src/components/templates/BaseTemplate.tsx": "Shared base template that renders hero, couple photos, events, gallery, RSVP form, and share section; updated to use the new `RSVPForm` props (`coupleNames` only) and new `ShareSection` props (`inviteId`, `coupleNames`, `weddingDate`).",
  "frontend/src/components/effects/FloatingPhotoFrame.tsx": "Floating photo frame component that replaces the old filigree overlay with a premium multi-layered CSS border using gold/accent-colored border, outline offset, and box-shadow glow effects; the photo is fully visible with no overlapping elements, and the floating bobbing animation and reduce-motion support are preserved.",
  "frontend/src/components/invitation/BackgroundMusic.tsx": "Background music player that attempts autoplay immediately on mount with a 300ms delay, gracefully falls back to a one-time document interaction listener if the browser blocks autoplay, and keeps the floating play/pause button visible and functional at all times without throwing unhandled promise rejections.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; mutation hooks (`useCreateInvite`, `useUpdateInvite`, `useSubmitRSVP`) now throw a descriptive user-friendly error message (\"Backend connection is not ready yet. Please wait a moment and try again.\") instead of the generic \"Actor not available\" when the actor is null.",
  "frontend/src/components/creator/StepPublish.tsx": "Publish step with upload progress tracking; uses a `useEffect`-based `actorSettled` flag with a 1.5s grace period to distinguish \"still connecting\" from \"truly unavailable\", preventing the false \"Backend connection unavailable\" banner from flashing immediately on mount before the actor query has even started. The button is disabled and shows \"Connecting…\" while the actor is initializing, and only shows an error if the user explicitly clicks publish while the actor is null.",
  "frontend/src/App.tsx": "Main app router with four routes (landing, create, invite display, RSVP responses) wrapped in ThemeProvider and QueryClientProvider.",
  "frontend/src/pages/RSVPResponsesPage.tsx": "Creator-only RSVP responses page that checks authentication and principal ownership before displaying response data; shows login prompt for unauthenticated users and Access Denied for non-creators.",
  "frontend/src/pages/LandingPage.tsx": "Conditionally renders a personal \"My Invitations\" dashboard (when logged in) with invite cards showing Edit/RSVP buttons, or the marketing landing page (when logged out) with hero, How It Works, and template gallery sections.",
  "frontend/src/components/layout/AppLayout.tsx": "Sticky-header layout shell with Internet Identity login/logout controls — shows abbreviated principal and Logout when authenticated, Login button when not; includes branded footer.",
  "frontend/src/components/invitation/RSVPForm.tsx": "RSVP submission form with name, email, attendance radio buttons, guest count select, and message textarea; all form elements use explicit inline style colors (white backgrounds, dark text) for readability on any template background, and uses the correctly named `useSubmitRSVP` hook.",
  "frontend/src/lib/templateRegistry.ts": "Template registry with 12 templates (8 standard + 4 premium), each with both `thumbnail` and `thumb` fields for compatibility; exports `TemplateComponents` map typed with `InviteRecord` (not `unknown`) and a `TEMPLATE_COMPONENT_MAP` alias.",
  "frontend/src/components/creator/StepTemplateSelector.tsx": "Template selector grid with light/dark variant toggle; premium templates display a gold \"Premium\" badge with Sparkles icon using the `thumbnail` field from the registry.",
  "frontend/src/components/templates/MughalGardenTemplate.tsx": "Premium Mughal Garden template using `TemplateProps` (`variant` prop) and delegating to `BaseTemplate` with emerald/gold hero styles and Mughal arch/jali SVG overlays as children.",
  "frontend/src/components/templates/VedicFireTemplate.tsx": "Premium Vedic Fire template using `TemplateProps` (`variant` prop) and delegating to `BaseTemplate` with amber/crimson hero styles and flame/yantra SVG overlays as children.",
  "frontend/src/components/templates/RoyalNavyZariTemplate.tsx": "Premium Royal Navy Zari template using `TemplateProps` (`variant` prop) and delegating to `BaseTemplate` with deep navy hero styles and gold zari-border/crest SVG overlays as children.",
  "frontend/src/components/templates/LotusBloomTemplate.tsx": "Premium Lotus Bloom template using `TemplateProps` (`variant` prop) and delegating to `BaseTemplate` with pink/ivory hero styles and layered lotus petal SVG overlays as children."
}